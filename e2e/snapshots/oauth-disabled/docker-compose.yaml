services:
  idp:
    build:
      context: ../../../
      dockerfile: Dockerfile
    volumes:
      - ./local-idp.config.yaml:/config.yaml:ro
    ports:
      - "8081:8081"
    environment:
      - PORT=8081
