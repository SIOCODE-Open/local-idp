services:
  idp:
    build:
      context: ../../../
      dockerfile: Dockerfile
    volumes:
      - ./local-idp.config.yaml:/config.yaml:ro
    ports:
      - "8086:8086"
    environment:
      - PORT=8086
